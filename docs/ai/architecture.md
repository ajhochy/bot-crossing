# Architecture

Vite/vanilla DOM + Three.js frontend polls a loopback Node API. Adapters read local stores; the scanner normalizes identities. The browser owns colony preferences with optimistic concurrency and three-way merge. Git discovery caches are server-owned, outside harness stores.
